# 手套到 Wuji 轨迹

数据流为：MCAP topic 检测 → 右手骨架解析 → 左右手坐标镜像 → 官方离线重定向 → 20 关节
规范轨迹 → 可选 NPZ/JointState MCAP。若输入已经包含标准 joint states，则直接读取，不能
仅靠文件名猜测格式。

规范轨迹要求二维 `(N, 20)`、全部有限、时间戳严格递增，并在仿真或真机消费前检查关节
范围与最大单步变化。外部官方源码和 SDK 只从 `local/vendor/` 解析，不复制进 Git。
