# 贴桌后退

`wuji-table-retreat` 在 hand-only MuJoCo 中保留录制指姿，只调整手掌使其准备阶段贴桌、
动作阶段按给定距离后退，并用 Z 投影防止穿桌。循环之间生成平滑 reset，最后停在终态。

安全约束包括掌心方向、拇指间隙、关节步长和整手最大穿透。参数不能绕过这些预检。
旧项目真实输入曾保留 499 帧并自动识别动作区间，但该数字只属于历史样本；新输入必须以
本次报告为准。
