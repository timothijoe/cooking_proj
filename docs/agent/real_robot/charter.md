# 真机总章程

真机代码默认不得运动。帮助、轨迹生成和离线预检应无需连接设备；需要读反馈的 dry-run
必须明确说明仍会连接。实际运动一律显式 `--execute`，并要求设备旁操作者确认物理急停、
空间、设备身份、工具/坐标、限位、反馈新鲜度和错误码。

控制流程必须具备连接超时、受控使能、限速/限步、运行期监控、异常停机和 `finally` 清理。
SDK A/B 臂、左右手及单位约定不得靠猜测。仿真通过不等于真机授权。

当前接口细节见 `evolution/`；人类操作从[真机教程](../../../tutorials/tianji_hardware.md)开始。
