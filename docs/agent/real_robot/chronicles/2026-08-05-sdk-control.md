# 2026-08-05：Tianji 与 Wuji SDK 真机控制形成

本阶段确认 Wuji 左手可通过 `wujihandpy` 读取身份、错误、温度、位置和关节限位，并形成
复位、姿态、轨迹回放工具。Tianji 侧从单臂实验发展到双臂关节阻抗播放，明确了 A/B 臂、
状态模式、K/D 配置和每周期批量发送的基本流程。

开发中暴露了 SDK API 名称、B 臂默认反馈、state=100 和模式切换等问题，因此后续接口加入
反馈帧、错误码、限位、缓入和异常清理检查。旧项目当时做过实体实验；迁移仓库尚未复验。
