# 2026-08：真机接口迁入轻量仓库

Tianji 的诊断、轨迹规划、切菜控制、点动和批流实验，以及 Wuji 的 SDK 契约、发送工具和
联合工作流被迁入分包结构。厂商 SDK 移到 `local/vendor/`，运行数据移到 `local/data/`，
虚拟环境改为本机重建而不是提交 Git。

迁移后的全量自动测试通过既定基线，但没有连接实体设备。由此得到的结论是代码边界和离线
行为被保留，而非现场运动、安全或性能已经验收。
