[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "cooking-tianji-arm"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = ["cooking-robot-core", "numpy>=2,<3"]
