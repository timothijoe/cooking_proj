[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "cooking-simulation"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = ["cooking-robot-core", "mujoco==3.10.0", "numpy>=2,<3"]

[project.scripts]
twin-sim = "twin_sim.cli:main"
